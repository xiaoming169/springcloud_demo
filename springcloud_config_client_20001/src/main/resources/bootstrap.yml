server:
  port: 20001

spring:
  cloud:
    config:
      #配置中心的地址
      uri: http://localhost:20000/
      #配置文件名称
      name: application
      #分支
      label: master
      #文件后缀
      profile: test